// For local development, keep this empty so the frontend uses the same host.
// For GitHub Pages, set this to your deployed FastAPI backend URL.
// Example: window.API_BASE_URL = "https://ai-trade-assistant-api.onrender.com";
window.API_BASE_URL = window.API_BASE_URL || "";
